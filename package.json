{
  "name": "next-js",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"json-server --watch db.json --routes routes.json --port 5305\" \"next dev\"",
    "build": "next build",
    "start": "concurrently \"json-server --watch db.json --routes routes.json --port 5305\" \"next start\"",
    "lint": "next lint"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^1.9.7",
    "json-server": "^0.17.4",
    "next": "14.0.1",
    "react": "^18",
    "react-dom": "^18",
    "react-redux": "^8.1.3"
  },
  "devDependencies": {
    "sass": "^1.69.5",
    "concurrently": "^8.2.2"
  }
}
